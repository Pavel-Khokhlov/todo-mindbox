import type { ValueCache } from '@linaria/tags';
import type Module from '../module';
import type { CodeCache, Options } from '../types';
/**
 * Evaluates template dependencies.
 */
export default function evalStage(resolveCache: Map<string, string>, codeCache: CodeCache, evalCache: Map<string, Module>, code: string[], options: Pick<Options, 'filename' | 'pluginOptions'>): [ValueCache, string[]] | null;
